- Keep endpoint that immediately returns files
- Have plot spectra store url as extra button (that will have to parse through files again
- remove invalid characters prevent SQLInjection


Priority of features:
Getting fits files
Plotting spectra
Getting JSON object

Documentation:
- doctrings for methods (inputs, outputs, types, sideffects)
- add descriptions to all endpoints
- For common applications, describe precedure (which endpoints to use)
